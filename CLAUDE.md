# CLAUDE.md — ONI Together 안정성 작업

이 파일은 fork (`younatics/Oxygen_Not_Included_Together`) 의 `mp-stability` 브랜치에만 있다.
upstream 에는 없다.

## 미션

이 모드는 잦은 disconnect / desync / host-client 상태 불일치가 있다. LAN(직접 IP)에서도 재현된다.
**목표는 원인을 고치는 것이고, 방법은 verifier-first 다** — 재현·측정 수단을 먼저 만들고 그 다음 고친다.

정적 감사가 이미 끝났다: **[AUDIT.md](AUDIT.md)** (3축 병렬 오딧 + 직접 재검증, 반증 9건 기록).
테스트 하네스도 준비돼 있다: **[testing/README.md](testing/README.md)**.

## 절대 규칙

1. **재현 없이 고치지 않는다.** 각 수정은 `testing/SCENARIOS.md` 의 시나리오 하나에 대응해야 하고,
   수정 전/후 `diff_logs.py` exit code 가 `1 → 0` 으로 바뀌는 것을 보여야 한다.
2. **두 PC 의 모드 DLL 은 byte-identical 이어야 한다.** 버전 핸드셰이크가 없어서 어긋난 쌍은
   조사 중인 버그와 똑같은 증상을 낸다. `deploy-to-peer.ps1` 이 sha256 을 검증한다.
   `collect-logs.ps1` 이 `*.meta.json` 에 해시를 기록한다 — 두 파일의 `modDllSha256` 이 다르면 그 실행은 버린다.
3. **Workshop 버전은 반드시 끈다.** 두 개 로드되면 Harmony 패치가 두 번 적용되어 결과가 위증된다.
4. **컴파일이 유일한 게이트다.** macOS 세션에서는 게임 DLL 이 없어 빌드를 못 한다 —
   Windows 세션이 `dotnet build` 를 통과시킨 것만 신뢰한다.
5. 한 번에 **한 가지만** 고친다. 감사 항목들은 서로 증상을 가린다.

## 반복된 오진과 그 방지책

전부 실제로 겪은 것들이다. 각 항목은 "무엇을 잘못 읽었는지 → 무엇으로 막는지" 다.
**추측이 아니라 이미 대가를 치른 목록이므로, 새 진단을 시작하기 전에 여기부터 본다.**

### 좋아 보이는 숫자가 "아무 일도 안 일어났다"는 뜻일 때

같은 형태로 **네 번** 속았다.

- 콜로니의 듀플이 다 죽어서 `lookupFails 0~1` → 활동이 없던 것
- 테스트가 클라를 세션에서 쫓아내 수치가 죽거나 부풀던 것
- `OnSpawn` 예외가 id 계층을 마비시켜 `4~6` 이던 것 (예외를 고치자 프레임 76ms→17ms, 실패 4→1700)
- 계측을 넣었는데 0건 → 그 DLL 에 계측이 없었던 것

**방지책:** 좋아진 값을 볼 때마다 **"그 일이 실제로 일어났는가"** 를 먼저 확인한다.
활동량 지표(`registry`, `dupes`, `previews`, 발동 카운터)가 0이 아닌지 보고 나서 판정한다.
`verify.ps1` 의 마커 검사가 "계측이 실린 DLL 로 돌았는가" 를 자동으로 막는다.

### 단위를 안 물어서 크기를 100배 틀림

`lookupFails` 하나를 다섯 번 다시 정의하며 **2,402 → 44** 로 좁혔다:

| 읽은 것 | 실제 |
|---|---|
| "분당 1700건 회귀" | 속도 4배 + 같은 id 반복 |
| 총량 비교 | 프레임 시간이 4배 다른 두 실행 |
| 총량 − 해결됨 = **-13** | 사건 수에서 객체 수를 뺐다 |
| 미해결 서로 다른 id 2,402 | 92%가 "일찍 온 패킷" |
| **진짜 누락 44~60** | 이것만이 고칠 대상 |

**방지책:** 새 숫자를 만들 때 **단위를 주석에 못 박는다**(사건/객체/비율). 총량이 아니라
행별 델타(`lookupFailsPerRow`, `framesThisRow`)로 비교한다. 그리고 **분류가 안 된 숫자로
코드를 고치지 않는다** — 이 규칙을 어겨 수정 셋이 연속 회귀였다.

### 로그 문구를 사실로 읽음

`is peer-agent.ps1 running on PC-B?` 를 보고 "에이전트가 꺼졌다" 고 단정했다. 실제로 측정된
것은 타임아웃뿐이었고 에이전트는 살아 있었다(배포 직후 ONI 재시작 처리 중).

**방지책:** 오류 메시지의 **추측 부분과 측정 부분을 구분**한다. 타임아웃이면 `peer-cmd -Verb ping`
으로 살아 있는지 30초 안에 직접 확인한다.

### 낡은 산출물을 새 결과로 읽음

`runs/` 의 이전 실행 로그, 공유 폴더의 옛 `client.log`, 게이트에 막혀 안 돈 실행의 직전 로그 —
**세 번** 낡은 파일을 새 결과로 보고했다.

**방지책:** 분석 전에 **로그 타임스탬프를 먼저 찍는다**(`LastWriteTime` vs 지금). 게이트가 막았으면
새 산출물이 없다는 뜻이므로 읽지 않는다.

### 한 증상이 독립 결함 여러 개일 때 — 하나만 고치면 "효과 없음"으로 보인다

"클라가 자원량을 다르게 본다"가 **서로 무관한 다섯 개 결함**이었다(syncer 미부착, 적용 불가한
형식, 0kg 비대칭, 키프레임 없음, 조립기 다중 컨테이너). 하나씩 고칠 때마다 `MASS 96 → 92 → 99`
로 지표가 안 움직였고, 두 번은 "이 수정은 이득이 아니다"라고 판단할 뻔했다.

실제로는 전부 필요했고, 다 모이자 **99 → 6, 5kg 이상 16 → 0** 이 됐다.

**방지책:** 지표가 안 움직였을 때 **"이 수정이 틀렸다"와 "다른 결함이 같은 지표를 붙잡고 있다"를
구분한다.** 구분 방법은 그 수정이 실제로 발동했는지를 **별도 카운터로** 보는 것이다
(`storeInPlace`, `kept`, `resyncs`, `multiStore` — 각 수정마다 자기 카운터를 붙였다).
발동했는데 지표가 안 움직이면 결함이 더 있다는 뜻이고, 되돌릴 이유가 아니다.

### null 검사가 막아주는 것처럼 보이지만 기본값을 실어 보낸다

여섯 개 패킷이 전부 이 모양이었다:

```csharp
if (ToolMenu.Instance?.PriorityScreen != null)
    Priority = ...GetLastSelectedPriority();   // null 이면 대입을 건너뛴다
...
writer.Write(Priority.priority_value);          // 그리고 그 미설정값을 보낸다
```

**가드는 예외만 막고, 안 채운 값이 그대로 전송된다.** 받는 쪽은 그 0을 자기 우선순위 화면에
밀어넣고 도구를 실행했다 → `Priority Value Out Of Range: 0`. 실측 `prioFixed=3~4/실행`.

**방지책:** 와이어에 나가는 값은 **"없으면 건너뛴다"가 아니라 "없으면 유효한 기본값"** 이어야 한다.
그리고 읽는 쪽이 범위를 검사해 **교정하고 센다** — 조용히 통과시키면 원인이 게임 로그에만 남는다.
`PriorityWire` 가 그 관문이다.

### 한 곳에서 잡힌 결함은 대개 같은 모양으로 여러 곳에 있다

오늘 네 번 반복됐다:

| 결함 | 같은 모양이 있던 곳 |
|---|---|
| 저장고 syncer 미부착 | 하드코딩된 **4개 타입**, 나머지 전부 누락 |
| identity 배제 규칙 | `OnSpawn` 에 놓았는데 `RegisterIdentity` 호출자 **91곳**이 우회 |
| 우선순위 0 전송 | 패킷 **6개** 전부 |
| 델타만 있고 키프레임 없음 | `StructureSyncerBase` 를 쓰는 **모든** syncer |

**방지책:** 결함을 하나 고치기 전에 **그 패턴을 Grep 한다.** 측정된 한 곳만 고치면 나머지는
다음 실행에서 다시 나오고, 그때는 "고쳤는데 또 난다"로 읽혀 진단이 더 어려워진다.

### 지표가 좋아진 게 실패가 다른 카운터로 옮겨간 것일 때

한 변경으로 클라 `lookupFails` 가 3614 → 1471 로 **절반 이하**가 됐다. 같은 실행에서
`NetId 0` 패킷이 **0 → 476** 이 됐고(96개 빌드 이력에 없던 값), 되돌리자 3514 로 복귀했다.

개선이 아니었다. **id 0 을 실은 패킷은 레지스트리 조회 전에 거부되므로 `lookupFails` 로
세지 않는다** — 실패가 사라진 게 아니라 `UnsetIdLookupCount` 라는 다른 이름으로 옮겨간 것이다.
좋아진 숫자 하나만 보면 성공으로 보이는 구조였다.

**방지책:** 한 카운터가 크게 좋아지면 **그 실패가 갈 수 있는 다른 카운터를 같이 읽는다.**
특히 "거부/차단"류 수정은 실패를 없애는 게 아니라 **더 이른 단계로 옮기는** 경우가 많다.
HEALTH 행을 통째로 diff 하고, 새로 0이 아니게 된 항목을 먼저 본다.

### 낡은 측정값에 수정을 겨눔

"클라에만 있는 기체·액체 44~60개" 를 근거로 설계·구현·배포·검증까지 한 라운드를 다 돌렸다.
결과는 **중립**이었다 — 변경은 발동했지만(기체 65~83개 제외) 불일치는 5,6 → 5,5. 원인은
**그 44~60 이 옛 빌드의 숫자**였고, 배포 직전 빌드는 이미 `CLIENT-ONLY 7, 11` 이었다는 것이다.
이전의 다른 수정이 이미 없앤 것을 다시 고치고 있었다.

**방지책:** 수정에 착수하기 전에 **그 근거 숫자를 현재 빌드에서 한 번 다시 잰다.** 근거가 며칠 전
로그면 그건 가설이지 측정이 아니다. `soak-summary` 를 실행마다 이름 붙여 보관해 두면
"직전 빌드의 같은 지표" 를 바로 비교할 수 있다(`soak-summary-<빌드>.txt`).

### 스냅샷 검사가 구조적으로 못 잡는 것

`"두 객체가 한 id 를 갖지 않는다"` 는 통과하는데 두 peer 는 어긋났다 — 충돌 상대가
**이미 죽은 객체**라 한 시점 검사로는 절대 안 보인다.

**방지책:** 검사를 쓸 때 **"이 검사가 못 보는 상태는 무엇인가"** 를 적는다. 시간 축이 있는
문제는 두 시점 비교(`compare-netids.ps1`)로만 판정한다.

### 측정 도구가 교란원일 때

**세 번** 있었다 — 테스트가 유발한 오류가 오류 게이트를 오염, 테스트 부하가 지연 측정을 오염
(순간 RTT 262ms vs 조용한 중앙값 58ms), 테스트가 세션을 죽여 동기화 측정을 오염(`role=solo` 7/7).

**방지책:** 측정값을 볼 때 **테스트 스위트가 돌고 있었는지** 확인한다(`UnitTestRegistry.IsRunning`).
스위트가 세션을 해체할 수 있는 패킷은 `PacketHandlerRobustnessTests.Excluded` 에 넣는다.

**네 번째 사례(오래 방치했던 것):** "호스트 Unity 오류 4건"이 여러 실행에 걸쳐 빨간불이었고
원인 미상으로 남겨뒀다. 정체는 `finishbuild` 이 재료 배달을 건너뛰어 `initialTemperature` 가 0으로
남은 것이었다 — `TileComplete has a temperature of zero`. **4건 = `-FinishBuilds 4`, 전부 같은
밀리초.** 게이트가 내가 만든 오류를 세고 있었다.

**추가 방지책:** 오류가 **같은 순간에 몰려 있고 개수가 시나리오 파라미터와 같으면** 그건 콜로니가
아니라 도구다. 개수·타임스탬프를 먼저 본다.

### 코드를 읽고 원인을 지목하면 대체로 틀린다

성과가 있던 진단은 **전부** 계측이었다 — 생성 시점 호출 스택, 두 peer 로그 나란히 놓기,
10초 시계열, 패킷 `sent/recv` 차이, 단계별 로그. 반대로 코드를 읽고 null 을 지목한 두 번,
원인을 지목한 다섯 번은 대부분 틀렸다(파이프·id 문제에서 가설 4개 연속 반증).

**방지책:** `"Object reference not set"` 처럼 위치를 안 주는 오류는 **단계별 로그**로 줄을 특정한다.
가설을 세웠으면 **그 가설이 틀렸을 때 무엇이 보일지**를 먼저 정하고 잰다.

### 되돌리는 것도 수정이다

세 수정이 회귀였고(입양 완화 → 맞는 객체 개명 8,184건 실패, 공지 인식 축소 → 실패율 증가,
id 은퇴 → 호스트 충돌 0→13), **되돌린 것이 옳은 조치였다.**

**방지책:** **측정된 비용 vs 측정되지 않은 이득**이면 되돌린다. 그리고 되돌릴 때 대가를
문서에 남긴다(예: 와이어 수리 미반영은 `REFACTOR_BACKLOG.md` 에 열린 항목으로).

### 게임 API 이름을 추측하지 않는다

한 함수를 쓰는 데 컴파일을 **세 번** 실패했다 — `IncubationMonitor.Instance` 를 Unity 객체로,
`GetAmounts` 를 GameObject 메서드로, `Modifiers` 를 존재하는 타입으로 각각 잘못 알았다.
정답은 이미 이 저장소 안에 있었다: `VitalStatsSyncer.cs:32` 이 `gameObject.GetAmounts()` 를
`using Klei.AI;` 와 함께 쓰고 있다.

**방지책:** 게임 API 를 쓰기 전에 **저장소에서 그 API 를 이미 쓰는 곳을 찾아 통째로 베낀다.**
`Grep` 한 번이 컴파일 왕복 세 번보다 싸다. 없으면 그때 문서를 본다.

### 컴파일 오류는 전부 본다

오류를 `Select-Object -First 3` 으로 잘라 읽어서, 같은 빌드에 함께 있던 오류를 다음 라운드에
다시 만났다. **왕복 횟수를 내가 늘리고 있었다.**

**방지책:** 빌드 출력의 `error CS` 는 **자르지 않고 전부** 읽는다. 길어도 한 번에 다 고치는 편이
싸다.

### 소스 편집에 PowerShell 치환을 쓰지 않는다

`-replace` / `.Replace()` 로 소스를 고치다 **두 번** 사고를 냈다(스크립트 파일이 자기 안에
중첩됨, 그리고 치환이 조용히 실패해 게이트가 막음). 이미 규칙으로 정해놓고 **또 어겼다.**

**방지책:** 소스·스크립트 편집은 **Edit 도구만** 쓴다. PowerShell 은 실행·조회 전용.

### 마커는 반드시 런타임 문자열

**두 번** 주석 문구를 마커로 잡아 게이트가 멀쩡한 빌드를 막았다.

**방지책:** 마커는 **HEALTH 행에 들어가는 카운터 이름**으로 잡는다. 그건 항상 리터럴이고
동시에 그 실행에서 볼 값이다.

## 수정 순서 (감사 결론)

| 순 | 대상 | 파일 | 크기 | 대응 시나리오 |
|---|---|---|---|---|
| 1 | 캔 광석 클라 2중 생성 | `Patches/World/WorldDamagePatch.cs:45` | **2줄** | S2 |
| 2 | 셀 단위 스택트레이스 로깅 → 호스트 동결 | `DebugTools/DebugConsole.cs`, `NetIdHelper.cs:47,78` | 1줄+삭제 | S6 |
| 3 | 하드싱크당 월드 N+1회 동기 저장 | `Misc/World/GameServerHardSync.cs:67` | 소 | S6 |
| 4 | 전체상태 패킷 페이징 | `PlantGrowthSyncer`, `WorldStateSyncer` | 중 | S3·S4 |
| 5 | NetId float 항 제거 **+ Unregister 소유자 확인 (반드시 같이)** | `NetIdHelper.cs:66`, `NetworkIdentityRegistry.cs:35` | 중 | S1 |

⚠️ **5번을 단독으로 적용하지 말 것.** float 항을 빼면 엔트로피가 줄어 충돌이 늘고,
`breakoff` 루프가 로컬 등록 순서에 의존하므로 새로운 divergence 를 만든다.
`Unregister` 를 소유자 확인형으로 바꾸는 것과 **한 커밋에** 가야 한다.

## Windows 명령

```powershell
# 최초 1회 (관리자 PowerShell)
cd testing
.\bootstrap.ps1 -Role host          # PC-A (dev/host)
.\bootstrap.ps1 -Role client        # PC-B

# 코드 수정 후 반복 루프
cd ..
dotnet build ONI_Together\ONI_Together.csproj -c Debug     # 자동으로 dev 모드 폴더에 배포
cd testing
.\deploy-to-peer.ps1 -PeerHost <PC-B IP> -PeerUser <계정>  # 클라에 밀어넣고 해시 검증
# → 양쪽 ONI 재시작 → 시나리오 실행 → 로그 수집 → diff_logs.py
```

## 이 코드베이스에서 알아둘 것

- **Transport 2종이고 비대칭이다.** Steam(`Transport/Steamworks/`)과 **Riptide=UDP**(`Transport/Riptide/`).
  Riptide 는 payload **1000 바이트** 한도가 있고 초과분을 `ChunkedPacket` 으로 쪼갠다.
  여러 syncer 가 주석에 "Steam 1200B MTU 에 맞춘다"고 적고 1100B 패킷을 만든다 → **LAN 에서만 조각화된다.**
  이 200바이트 차이가 최상위 버그 여러 개의 원인이다.
- **패킷 헤더에 sequence·tick·sender 가 없다.** `int packetType` 4바이트뿐. 그래서 손실·중복·재정렬을
  사후에 증명할 수 없다. AUDIT.md §7 이 최소 계측안이다.
- **핸들러 로직은 패킷 클래스의 `OnDispatched()` 안에 있다.** 별도 dispatch 테이블이 없다.
- **엔티티 생성은 복제되지 않는다** (`KInstantiatePatch` 의 큐 호출이 주석 처리됨). 양쪽이 각자
  객체를 만들고 각자 ID 를 발급한다 — 이것이 NetId 버그를 치명적으로 만드는 전제다.
- **클라 AI 는 꺼져 있다** (`ChoreConsumer`·`MinionBrain`·`CreatureBrain`, `AdvancePath` 차단).
  즉 divergence 는 "독립 AI" 가 아니라 **미복제/이중복제 이벤트**에서 온다.
- `Profiler.Scope()` 는 release 에서 no-op 이다. 단 **DEBUG 빌드는 호출마다 문자열 작업을 해서
  성능 측정에 쓸 수 없다** — perf 를 재려면 Release 로 빌드한다.

## 이미 알려진 함정 (버그로 인지하고 있어야 함)

- **재접속: 고쳐졌다(2026-08-12, 측정 확인).** 두 개의 별개 결함이었다.
  1) 재접속 **대상 주소**가 `127.0.0.1:7777` 로 덮이던 것 — `GameClient._cachedConnectionInfo` 로 해결됨.
  2) **더 심각했던 것**: `NetworkIdentityRegistry.Clear()` 가 자기 딕셔너리만 비우고
     컴포넌트의 `NetId` 와 `IsRegistered=true` 를 남겼다. `RegisterIdentity()` 첫 줄이
     `if (IsRegistered && NetId != 0) return;` 이므로 **8000개 객체가 "나는 등록됐다"고 믿고
     레지스트리는 아무것도 모르는** 상태가 됐다. 등록은 `OnSpawn` 뿐이고 재접속엔 스폰이 없다.
     측정: 클라 `registry 8085 → 41`, `lookupFails 4,586 → 90,068`, 공유 id `8105 → 31`,
     세계는 온전(듀플 21, 식물 343). **연결은 정상이고 아무것도 지목할 수 없었다.**
     해결: `Clear()` 가 `ForgetRegistration()` 으로 객체에게도 알리고(단 `NetId` 는 유지),
     `ReattachAll()` 이 같은 번호로 다시 등록한다. 결과 `registry 8172`, `reattached 8189`,
     `HOST-ONLY 8019 → 153`(정상 실행과 동일).
  **이걸 잡은 것은 라이브 재접속 시나리오뿐이다.** 원인에 대한 단위 테스트(`ReconnectTests`)는
  있었고 통과하고 있었다 — 주소만 확인했고 그 뒤에 무슨 일이 나는지는 아무도 안 봤다.
  이제 `RegistryPopulationTests` 가 **레지스트리를 세계와 비교**해서 한 대에서 잡는다.
- **로딩 중 끊기면 LAN 은 메인 메뉴로 강퇴된다.** Steam 경로(`SteamworksClient.cs:240-244`)에는
  `LoadingWorld` 가드가 있는데 Riptide 에는 없다.
- **TCP 8081 이 막히면 조용히 UDP 청크 폴백으로 떨어진다** — 그게 버그 밀집 경로다. 방화벽 먼저.
